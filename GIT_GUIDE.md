# คู่มือการใช้งาน Git ฉบับเข้าใจง่าย (สำหรับโปรเจกต์ VocabFlow)

คู่มือนี้สรุปวิธีใช้งาน Git เพื่อให้คุณไม่ต้องกลัวโค้ดพัง สามารถทดสอบได้อย่างมั่นใจ และย้อนกลับได้เสมอ

---

## 1. วิธีทำงานร่วมกับ AI (แนะนำ - ง่ายที่สุด ไม่ต้องพิมพ์คำสั่งเอง)

เพื่อให้มั่นใจว่า **"จะเซฟเฉพาะตอนที่คุณเทสแล้วว่าผ่านจริงเท่านั้น"** ให้ใช้ขั้นตอนดังนี้ครับ:

```
[1. สั่ง AI แก้ไขโค้ด]
        ↓
[2. คุณเปิดทดสอบใช้งานหน้าเว็บ]
        ↓
   ┌────┴────────────────────────┐
   ↓                             ↓
(ถ้าเทสแล้วผ่าน/พอใจ)       (ถ้าเทสแล้วพัง/ไม่ชอบ)
   ↓                             ↓
สั่ง: "เทสแล้ว OK บันทึก git ให้ที"    สั่ง: "พัง/ไม่เอา ย้อนกลับเป็นค่าเดิมให้ที"
(AI จะทำการ git commit ให้)     (AI จะทำการ restore ให้กลับมาเหมือนเดิมทันที)
```

### ตัวอย่างคำสั่งที่คุณพิมพ์บอก AI ได้เลย:
- **เมื่อเทสผ่านแล้วต้องการบันทึก:**
  > *"เทสระบบเสียงแล้วใช้งานได้ดี ช่วย commit ให้หน่อยว่า 'เพิ่มระบบเสียงอ่านคำศัพท์'"*
- **เมื่อระบบมีปัญหาและอยากย้อนกลับจุดเดิม:**
  > *"หน้านี้พัง ย้อนโค้ดกลับไปจุดเซฟล่าสุดให้หน่อย"*
- **เมื่ออยากเช็คว่าตอนนี้แก้ไฟล์ไหนไปบ้าง:**
  > *"ตอนนี้มีไฟล์ไหนที่แก้ค้างอยู่บ้าง เช็ค git status ให้หน่อย"*

---

## 2. วิธีใช้งานด้วยตัวเองผ่าน Terminal (PowerShell / Command Prompt)

หากคุณต้องการเปิด Terminal ทำเองในโฟลเดอร์โปรเจกต์ สามารถใช้คำสั่งพื้นฐานดังนี้:

### 2.1 ตรวจสอบสถานะ
- **ดูว่ามีไฟล์อะไรถูกแก้ไขบ้าง:**
  ```powershell
  git status
  ```
- **ดูเนื้อหาบรรทัดที่ถูกแก้ไขเทียบกับจุดเซฟล่าสุด:**
  ```powershell
  git diff
  ```

---

### 2.2 เมื่อทำพัง แล้วต้องการ "ย้อนกลับ" ทันที

- **ย้อนกลับไฟล์ทั้งหมดที่แก้ไป (แต่ยังไม่ได้ commit) ให้กลับมาเป็นจุดเดิมล่าสุด:**
  ```powershell
  git restore .
  ```
- **ลบไฟล์ใหม่ที่เพิ่งสร้างและยังไม่ได้เซฟ (ถ้ามี):**
  ```powershell
  git clean -fd
  ```
- **ย้อนกลับเฉพาะไฟล์ใดไฟล์หนึ่ง:**
  ```powershell
  git restore <พาธไฟล์>
  # เช่น: git restore src/app/page.tsx
  ```

---

### 2.3 เมื่อทดสอบผ่านแล้ว ต้องการ "เซฟบันทึกจุดเช็คพอยต์ (Commit)"

- **ขั้นตอนเซฟ:**
  ```powershell
  git add .
  git commit -m "คำอธิบายสิ่งที่คุณทำ เช่น: แก้ไขสีปุ่มหน้าทบทวนคำศัพท์"
  ```

---

### 2.4 ดูประวัติการเซฟย้อนหลัง

- **ดูรายการประวัติที่เคยเซฟไว้แบบกระชับ:**
  ```powershell
  git log --oneline
  ```
  *(ตัวอย่างผลลัพธ์)*
  ```
  3b68292 Initial commit: VocabFlow application setup
  ```

---

### 2.5 ย้อนกลับไปเวอร์ชันก่อนหน้าในอดีต (ย้อนเวลา)

ถ้าคุณ commit ไปแล้วหลายรอบ แต่อยากย้อนทั้งโปรเจกต์กลับไปที่ commit ในอดีต:
```powershell
# 1. ดูรหัส commit ด้วยคำสั่ง
git log --oneline

# 2. ย้อนเวลากลับไปจุดนั้น (แทนที่ xxxxxxx ด้วยรหัส commit ที่ต้องการ)
git reset --hard xxxxxxx
```
*(คำเตือน: `git reset --hard` จะลบการเปลี่ยนแปลงที่ทำหลังจากจุดนั้นทิ้งทั้งหมด)*

---

### 2.6 สำรองข้อมูลขึ้น GitHub (Cloud Backup)

โปรเจกต์นี้เชื่อมต่อกับ GitHub เรียบร้อยแล้วที่:
`https://github.com/wanchana1803-ai/vocabflow.git`

เมื่อคุณ commit บันทึกในเครื่องแล้ว และต้องการส่งขึ้น GitHub:
```powershell
git push
```
หรือสั่ง AI:
> *"เทสแล้วผ่าน ช่วย commit และ push ขึ้น GitHub ให้ด้วย"*

---

### 2.7 ดึงข้อมูลล่าสุดจาก GitHub ลงมาที่เครื่อง (Git Pull)

คำสั่ง **`git pull`** ใช้สำหรับ **"ดึงโค้ดล่าสุดบน GitHub กลับลงมาอัปเดตเครื่องเรา"**

#### ใช้เมื่อไหร่?
1. **ทำงานหลายเครื่อง:** เช่น ตอนกลางวันแก้โค้ดบนโน้ตบุ๊กแล้ว push ขึ้น GitHub พอตอนเย็นกลับมาเปิดคอมตั้งโต๊ะที่บ้าน ต้องกด pull เพื่อเอาโค้ดล่าสุดมาทำงานต่อ
2. **แก้ไขไฟล์บนเว็บ GitHub โดยตรง:** เช่น เข้าไปแก้ไฟล์ข้อความหรือ README บนหน้าเว็บ แล้วอยากให้ในเครื่องอัปเดตตาม
3. **ทำงานร่วมกับคนอื่น:** เมื่อเพื่อนร่วมทีม push โค้ดใหม่ขึ้นไป

#### วิธีใช้งาน:
```powershell
# 1. เช็คก่อนว่าในเครื่องเราไม่มีโค้ดค้างที่ยังไม่ได้เซฟ
git status

# 2. ดึงโค้ดเวอร์ชันล่าสุดจาก GitHub ลงมา
git pull
```

#### ข้อควรระวังก่อน Pull:
- **เคลียร์โค้ดในเครื่องให้เรียบร้อยก่อนเสมอ:** ตรวจสอบด้วย `git status` ถ้ามีไฟล์ที่แก้ค้างอยู่ ให้ทำการ `commit` หรือ `restore` ทิ้งไปก่อน ไม่อย่างนั้น Git อาจแจ้งเตือน Conflict (โค้ดชนกัน) ได้
- หรือสั่ง AI:
  > *"ช่วยดึงโค้ดล่าสุดจาก GitHub ลงมาหน่อย (git pull)"*

---

## สรุปกฎสำคัญ
1. **โค้ดที่ยังไม่ commit** = ย้อนกลับง่ายมากด้วย `git restore .`
2. **Commit เมื่อทดสอบแล้วว่าเวิร์ก** = สร้างเป็น checkpoint บันทึกความก้าวหน้าในเครื่อง
3. **Push ขึ้น GitHub** = ส่งโค้ดขึ้นคลาวด์ สำรองข้อมูล ปลอดภัยแม้คอมพัง
4. **Pull ลงจาก GitHub** = ดึงโค้ดล่าสุดจากคลาวด์ลงมาอัปเดตคอมพิวเตอร์
5. ให้ AI ช่วยจัดการได้ตลอดเวลา เพียงแจ้งว่า **"เทสแล้ว OK บันทึกให้ที"**, **"push ให้ที"**, **"pull ให้ที"** หรือ **"ย้อนกลับให้ที"**
